# El origen de datos, provisionado y no pinchado a mano: un Grafana configurado desde su pantalla
# vive en su volumen, y el volumen se borra.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
